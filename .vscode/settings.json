{
    "chat.tools.terminal.autoApprove": {
        "/^command -v firebase && firebase --version && command -v flutterfire && flutterfire --version && flutter --version$/": {
            "approve": true,
            "matchCommandLine": true
        }
    }
}